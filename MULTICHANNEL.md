# Мультиканальность («мультиюзинг») — план эпика

**Цель (запрос пользователя 2026-07-08):** превратить плоский одно-канальный софт в
мультиканальный. 4 независимых канала в одном приложении, каждый со своими источниками,
оформлением, фильтрами, лимитом и целевыми каналами публикации. Это шаг к «супер-софту»
и продукту (позже — мультиюзер/биллинг для чужих).

## 4 канала пользователя

| # | Канал | Источники | Оформление | Особое |
|---|-------|-----------|------------|--------|
| 1 | **Новости** (текущий) | TG `@NewsThreeWord`-источники + VK | как есть (keep_original) | работает на проде |
| 2 | **Кино** | VK «Кинопремьеры» (kinopremyeri) + др. | монтаж: белая плашка + заголовок КАПСОМ сверху + свой лого в углу (референс KINOPREMYERI) | фильтр off, 3–4 поста/день |
| 3 | **Городской (Партизанск)** | TG `ves_partizansk`, `partizansk_go`; сайт `partizansk.org/news`; VK `club211338495`, `maukgdkpart`, `partizansk_group_25`, `partizansk.town.county` | мощные хуки в тексте, привлекательные заголовки | **анти-реклама фильтр** (LLM: реклама/деньги→отсечь, если не реклама то ок), **кросс-дедуп между источниками**, только посты, БЕЗ видео |
| 4 | **Мемы** | VK `bog_memes` | вотермарк + 2 хэштега | всё видео → уникализация, больше всего клипов |

**Приёмники (куда публиковать)** — от пользователя ещё нужны: кино-канал, городской паблик,
мем-канал (TG @username + бот админом / VK group_id + токен группы в `.env`).

## Архитектура: плоское → мультиканальное

Сейчас всё глобально в `config.yaml` (`AppConfig`): один поток источников → одна пара
таргетов (TG+VK) → одно оформление/фильтр/лимит. Точки расслоения:
- `Source` (models.py) — нет привязки к каналу → `channel_id`.
- `run_check_cycle` (check_cycle.py) — перебирает все источники в один поток → перебор ПО КАНАЛАМ.
- `_publish_to_all` (headless_service.py) — публикует в глобальные таргеты → таргеты канала.
- Очередь/лимит/rate_guard (`list_fresh_queued_posts`, `count_published_since`,
  `mark_published`) — глобальные → per-channel.
- `AppConfig` — станет ДЕФОЛТАМИ; канал переопределяет своим блоком настроек.

### Модель данных
- Новая таблица `Channel`: имя, enabled, таргеты (tg/vk: token_env + destination,
  vk_upload_token_env), `settings_json` (Text — фильтр/оформление/лимит/стиль, гибко без
  миграций схемы под каждую настройку).
- `Source.channel_id` → FK channels.id.
- `ProcessedPost` — маршрутизация в таргет канала через raw_post→source→channel
  (в срезе очереди, возможно, добавить channel_id для лимита).
- Секреты — только имена env-переменных в БД (инвариант), токены в `.env`.

## Вертикальные срезы

- [x] **0a — фундамент данных.** Таблица `Channel` + `Source.channel_id` + CRUD +
  миграция текущих источников в «Канал 1 (Новости)». Прод не меняет поведение.
  Готово 2026-07-08 (коммит 595b75d), 461 тест зелёный.
- [x] **0b — расслоение публикации.** Читалка (fetcher) общая, ПУБЛИКАЦИЯ раздельная:
  `cycle_job` перебирает `repo.list_channels(enabled_only)`, берёт посты канала
  (`list_fresh_queued_posts(channel_id=)` через join source→channel) и публикует в его
  таргеты (`build_*_publisher_for_channel` по token_env канала, кэш по env). Сеть
  пропускается, если у канала не задан её destination (кино/мемы пока только VK).
  `_sync_default_channel_targets` заполняет Канал 1 из глобального config при старте
  headless — новости постят как раньше. Готово 2026-07-08, 464 теста.
  **Остаётся в след. срезах:** `ChannelSettings` (парсинг settings_json — фильтр/оформление/
  лимит per-channel); per-channel rate_guard (сейчас count/interval глобальные — при
  лимите 999/0 не мешает, кино-лимит 3–4 введём с ChannelSettings в срезе 1).
- [x] **1 — кино-канал живьём** (без монтажа). `ChannelSettings` (settings_json:
  `filters_enabled`, `max_posts_per_day`); pipeline ветка «лить всё» (пропуск LLM-гейта
  новостей и min_score, дедуп сохранён); check_cycle применяет настройки канала источника;
  второй владелец бота (`CONTROL_BOT_EXTRA_OWNER_IDS`); seed кино-канала
  (`app/seed_channels.py`: VK 240120678, источник VK «Кинопремьеры» 58170807, фильтр off,
  4/день, медиа оригиналом через keep_original). Готово 2026-07-09, 473 теста.
  TG-приёмник `@kinobestfilmss` пока не подключён (по запросу — сначала только VK).
- [ ] **2 — кино-монтаж** под референс (белая плашка + заголовок + свой лого).
- [x] **1.5 — per-channel rate_guard (защита от бана + изоляция каналов).** Дневной лимит
  и счётчик считаются по публикациям КОНКРЕТНОГО канала (`count_published_since(channel_id=)`,
  `get_last_published_at(channel_id=)` через join source→channel); `check_publish_allowed`/
  `publish_queued_post*` принимают `channel_id`; headless берёт лимит из `ChannelSettings.
  max_posts_per_day` (кино 4/день) или глобальный. Кино не заспамит VK лимитом другого
  канала. Готово 2026-07-09, 474 теста. (LLM-throttle уже был: Groq 20с/вызов + 429 backoff.)
- [ ] **2.5 — свой поиск фото по фильму** (Google/др. API): парсинг названия фильма из
  поста → поиск кадров → замена медиа источника. Новое требование пользователя 2026-07-09.
  Плюс per-channel `keep_original=false` (сейчас глобальный).
- [ ] **3 — городской канал.** Веб-фетчер (`partizansk.org/news`, HTML/RSS) + TG-источники,
  анти-реклама LLM-фильтр, кросс-дедуп между источниками канала, хуки в рерайте, без видео.
- [ ] **4 — мем-канал.** VK `bog_memes` → уникализация видео + вотермарк + хэштеги.
- [x] **5 — управление каналами в боте** (срез 1). Кнопка «📺 Каналы» → список → карточка
  канала: вкл/выкл канала, лимит/день, интервал, фильтр вкл/выкл (значения на кнопках,
  ввод через FSM), источники канала (вкл/выкл через тот же sources_menu с channel_id).
  Чистые функции `render_channels`/`render_channel_card`/`toggle_channel`/
  `toggle_channel_filter`/`set_channel_setting` (`control_bot.py`). Готово 2026-07-09,
  486 тестов. **Следующие срезы бота:** добавление источника СРАЗУ в канал (сейчас
  add_source без channel_id — глобальный), редактирование таргетов/токенов канала,
  создание нового канала с телефона, футер/keep_original per-channel в UI.

## Известные грабли мультиканальности
- **Включение ранее выключенного источника = бэклог пачкой.** toporlive включили
  2026-07-09 — `get_max_external_id` дал старый курсор, `fetch_new_posts` вернул 36
  накопленных постов. Спасает per-channel `min_interval_minutes` (новостям задан 15 мин) —
  посты уходят по одному, не пачкой. При включении старого источника: либо сразу задать
  каналу интервал, либо сдвинуть курсор на свежий.
- **Один источник может молчать.** Новости зависели от одного `novosti_efir` — он замолчал
  на ~10ч, канал ничего не публиковал. Держать 2+ активных источника на канал.
- **Нельзя включать канал в прод до полной готовности** (см. память feedback) — кино
  выложило 10 постов пачкой, включённое без монтажа/лимита.

## Новые технические вызовы (не было в проекте)
- **Веб-фетчер** для сайтов (Партизанск) — новый тип источника, не соцсеть.
- **Кросс-дедуп** между источниками одного канала (SimHash есть, развернуть на канал).
- **Анти-реклама LLM-классификация** для городского канала.
- **Per-channel лимит** — rate_guard считает по каналу, не глобально.
